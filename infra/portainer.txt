script: |
  docker stop portainer
  docker rm portainer

  docker run -d \
    # -p 8000:8000 \ # used for Edge Agent communication
    -p 9443:9443 \
    --name=portainer \
    --hostname=portainer \
    --restart=always \
    # --sslcert /path/to/cert/portainer.crt \
    # --sslkey /path/to/cert/portainer.key \
    -v /var/run/docker.sock:/var/run/docker.sock \
    -v portainer_data:/data \
    # portainer/portainer-ee:sts # analog to latest
    portainer/portainer-ee:2.20.3

docker run -d -p 9443:9443 --name portainer --hostname=portainer --restart=always -v /var/run/docker.sock:/var/run/docker.sock -v portainer_data:/data portainer/portainer-ee:2.20.3
